import configuratorReducer from './reducers';

export { default as configuratorTypes } from './types';
export { default as configuratorActions } from './actions';
export { default as configuratorSelectors } from './selectors';

export default configuratorReducer;
